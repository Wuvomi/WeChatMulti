# Swift / SwiftPM build artifacts
.build/
DerivedData/
*.o
*.swiftmodule
.swiftpm/xcode/

# Built app bundle (regenerated by build.sh)
*.app/

# Reverse-engineering binary dumps (large / not source; one exceeds GitHub's 100MB limit)
*.bin
re/bins/

# macOS
.DS_Store

# Logs
*.log
